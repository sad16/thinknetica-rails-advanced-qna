$("#question-id-<%= @question.id %> .edit-question-form .question-errors").html("<%= render "shared/errors", resource: @question %>");

<% if @question.errors.empty? %>
  $("#question-id-<%= @question.id %>").remove();
  $(".question-container").append("<%= j render @question %>")
<% end %>

loadGists();
